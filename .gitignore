# === Node / Next (build artefacts) ===
node_modules/
.next/
out/
coverage/
build/
dist/
*.tsbuildinfo
next-env.d.ts
playwright-report/
.storybook-out/
storybook-static/
*storybook.log

# === Env ===
.env
.env.*
!.env.example

# === macOS / misc ===
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === Yarn v2+ ===
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# === Vercel ===
.vercel/

# === Rust/wasm-pack artefacts ===
# общий target для любых крейтов
**/target/
# сгенерированный пакет wasm-pack (ESM + .wasm)
src/wasm/**/pkg/
# сырой .wasm если где-то оставили вне pkg
src/wasm/**/*.wasm

builds

# === IDE ===
.vscode/
.idea/